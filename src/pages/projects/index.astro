---
import Base from "../../../src/layouts/Base.astro";
const pageTitle = '项目 · Kateiso';
const pageDesc = '聚焦 AI into XR 与AI into的两个主线项目。';
const projects = [
  {
    title: 'HACM 人机共忆',
    role: 'AI into XR',
    period: 'Alpha · 2025',
    summary: '通过 World Generate 技术，让人能在 XR 场景下重新回到 过往记忆中',
    tags: ['XR', 'HACM', 'Experience']
  },
  {
    title: 'AI into Dining',
    role: '餐饮数字化',
    period: '2024 ～ 至今',
    summary: '统一品牌设计、全域套餐体系搭建、自动短视频发布工作流、获客→留存→复购循环。',
    tags: ['Hospitality', 'Ops', 'Forecast']
  }
];
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebPage',
  name: pageTitle,
  description: pageDesc,
  inLanguage: 'zh-Hans',
  url: 'https://kateiso.dev/projects.html'
};
---
<Base
  title={pageTitle}
  description={pageDesc}
  lang="zh-Hans"
  canonical="/projects.html"
  alternates={[{ href: '/en/projects.html', hrefLang: 'en' }]}
  languageToggle={{ href: '/en/projects.html', label: 'EN', ariaLabel: 'View English Projects' }}
  jsonLd={jsonLd}
>
  <section class="section">
    <div class="section__intro">
      <p class="hero__kicker">Projects</p>
      <h1>项目</h1>
      <p class="section__lede">所有工作都 AI into XR 以及 AI into Dining 展开。</p>
    </div>
    <div class="project-grid">
      {projects.map((item) => (
        <article class="project-card">
          <div class="project-card__meta">{item.role} · {item.period}</div>
          <h2 class="project-card__title">{item.title}</h2>
          <p class="project-card__summary">{item.summary}</p>
          <div class="project-card__tags">
            {item.tags.map((tag) => <span class="pill">{tag}</span>)}
          </div>
        </article>
      ))}
    </div>
  </section>


  </section>
</Base>
